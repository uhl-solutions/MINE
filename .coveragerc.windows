[run]
branch = True
relative_files = True

[report]
# Standard exclude patterns plus POSIX-only code (running on Windows)
exclude_lines =
    pragma: no cover
    pragma: posix-only
    if __name__ == .__main__.:
    raise NotImplementedError
    if TYPE_CHECKING:
